---
# Traefik 設定ディレクトリ
traefik_install_dir: /opt/traefik

# Docker イメージ
traefik_image: traefik:v3.3.3

# HTTP ポート
traefik_http_port: 80

# Docker API バージョン（ホストの Docker デーモンに合わせる）
traefik_docker_api_version: '1.45'
